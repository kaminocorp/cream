# Base URL of the Cream API server (no trailing slash).
NEXT_PUBLIC_API_URL=http://localhost:3001

# API key for the dashboard service account agent.
# In production this is an operator-level credential, not an agent credential.
# Stored server-side only — never expose in NEXT_PUBLIC_* variables.
CREAM_API_KEY=your-api-key-here
